def y ( lista):
    for x in range(1,10):
        lista.append(x)
    print(lista)
def z(lista,r):
    y = (-1)
    for x in lista:
        r.append(lista[y])
        y = y - 1
    print(r)
    
def main():
    r = []
    lista = []
    y(lista)
    b(lista,r)
main()
